language:
  - c
compiler:
  - clang
  - gcc
arch:
  - amd64
  - ppc64le
  - arm64
os:
  - linux
  - osx
  - windows
script:
- |-
    case $TRAVIS_OS_NAME in
      windows)
        nmake -f builds\win32-visualc.mk
      ;;
      *)
        make
      ;;
    esac
